# Supabase (browser-safe keys only — never commit service_role here)
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLIC_KEY=

# Cloudflare Turnstile — public site key; secret key goes in Supabase Edge Function secrets only
VITE_TURNSTILE_SITE_KEY=

# Contact form inbox (EmailJS) — set ONLY as Supabase Edge Function secrets for submit-contact:
#   EMAILJS_SERVICE_ID, EMAILJS_TEMPLATE_ID, EMAILJS_PUBLIC_KEY (Public Key / user_id)
# Optional server-side restriction: EMAILJS_PRIVATE_KEY maps to REST accessToken
# Restrict allowed origins in the EmailJS dashboard. Template vars: {{from_name}}, {{from_email}}, {{subject}}, {{message}}

# Optional dev-only — prefilled Admin Login password (omit in production/Vercel)
# Prefer pasting once into Login.tsx DEV_LOGIN_PASSWORD; env overrides when set:
# VITE_DEV_LOGIN_PASSWORD=your-local-password
